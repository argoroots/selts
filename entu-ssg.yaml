# locales:
#     - et
# defaultLocale: et

source: ./tmp_source
build: ./build
assets: ./assets

protectedFromCleanup:
    - .git
    - assets
    - index.html

pug:
    basedir: ./tmp_source/_templates

server:
    port: 4000
    assets: /assets/

dev:
    aliases: true
    paths:
